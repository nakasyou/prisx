{
  "name": "prisx-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --cwd apps/prisx dev",
    "dev:server": "bun run --cwd apps/prisx dev:server",
    "build": "bun run --cwd apps/prisx build && bun run --cwd packages/cli build",
    "start": "bun run --cwd apps/prisx start",
    "test": "bun run --cwd apps/prisx test && bun run --cwd packages/cli test",
    "typecheck": "bun run --cwd apps/prisx typecheck && bun run --cwd packages/cli typecheck",
    "cli": "bun run packages/cli/src/index.ts",
    "format": "bun run --cwd apps/prisx format && bun run --cwd packages/cli format"
  }
}
